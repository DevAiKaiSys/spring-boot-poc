package com.example.websocketredis.service;

import org.springframework.stereotype.Service;

@Service
public class ProjectService {

    // โค้ดน้อยที่สุด: แค่สร้างเมธอดให้มีอยู่จริง และ return ค่า default ไปก่อน
    public boolean checkExists(String customer, String projectName) {
        // Logic จริงๆ จะมาทีหลัง ตอนนี้ขอแค่ return อะไรก็ได้เพื่อให้คอมไพล์ผ่าน
        return true;
    }
}